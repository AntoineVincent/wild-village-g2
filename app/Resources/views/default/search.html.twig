{% extends 'base.html.twig' %}

{% block body %}
	<div class="col-md-12">
		<h3 class="searchtitle"> Résultat de la recherche :</h3>
	<div class="col-md-1"></div>
	<div class="col-md-10">
		{% for tabsearch in tabsearchs %}
		<div class="col-md-4 rslt-bgrd">
			{% if tabsearch.gender == "femme" %}
 		        <img class="profilerwilder" src="{{ asset('img/deer2_small.png') }}">
 		    {% elseif tabsearch.gender == "homme" %}
 		        <img class="profilerwilder" src="{{ asset('img/deer_small.png') }}">
 		    {% else %}
 		        <img class="profilerwilder" src="{{ asset('img/deer3.png') }}">
 		    {% endif %}
			<p class="rslt-search">Pseudo : {{ tabsearch['username'] }}</p>
			<p class="rslt-search">Derniere connexion: {{ tabsearch['lastlogin']|date('d-m-Y') }}</p>
			<p class="btn-profiler"><a href="{{ path('user_ami', { 'idami': tabsearch['id'] }) }}" class="link-profiler">Voir Profil</a></p>
		</div>
		{% endfor %}
	</div>
	<div class="col-md-1"></div>

{% endblock %}