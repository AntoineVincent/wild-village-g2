{% extends 'base.html.twig' %}

{% block body %}
    <h3>Vous êtes sur le point de supprimer définitivement le message suivant:</h3></br>
    
    <div class="col-md-6 suppressionform">
        <ul class="deletemessage">
            <h4>Message de <b>{{ message.usersent }}</b> à <b>{{ message.userdest }}</b> - le {{ message.date|date('Y-m-d H:i:s') }}</h4>
            <p>{{ message.content }}</p>
        </ul>
        <ul class="deletemessage">
            <li>
                {{ form_start(delete_form) }} <input type="submit" value="Confirmer la suppression"> {{ form_end(delete_form) }}
            </li>
            <li>
                <a href="{{ path('mess_mess') }}">Ne pas supprimer et revenir à la messagerie</a>
            </li>
        </ul>
    </div>
    <div class="col-md-6 suppressionimage">
        <img src="{{ asset('img/suppression.png') }}">
    </div>

{% endblock %}